SUBDIRS =	replace
if WITH_LIBESTDRAFT
SUBDIRS +=	libestdraft
endif

SUBDIRS +=	mailestd

EXTRA_DIST = README.md Makefile Makefile.inc libestdraft/Makefile
EXTRA_DIST += mailestd/Makefile compat.h mailestd.conf
